version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo Installing packages
      - npm install
      - echo Running build
      - npm run build

artifacts:
  files:
    - '**/*'